============================================
MAIN
============================================
- Installation of tools (PyCharm + Initial code + running first)
- Presentation of the environment (process.py : __init__, setup, update, draw) + game loop
- Presentation of the Class fields (access to these variables in all the process.py file) + self keywoard
- Presentation of the Sprites objects, creation + members (center_x,center_y,scale_x,_scale_y, draw(), update_animation())
- Information on creating new methods for each functionality (creation, draw, update, ...)
- Information on keeping a list of all the variables (types, names, descriptions, ...)
- Follow steps to create the game


============================================
STEPS
============================================
- single sprite + draw (creation + modify center_xy + angle + update_animation + draw)
- add position variable
- key event to update position + update sprite onto position + draw sprite
- controller event to update position
- anim sprite enable animation when moving and idle when stopped
- add background sprite
----------------------------
- add item creation function (speed and initPosition) (+activation when space bar is pressed : debug) -> use a list
- add item update(move) function (browse the whole list)
- add item removal from list when outside area
- modify code to generate items regularly (add timer and a function to reset/update it)
----------------------------
- add item removal when colliding with player (create box collision function)
- add score variable
- update score when player catches items (when collision is ok)
- add score text display
- play sound when catching an item
----------------------------
- add Life variable + function to reset it
- add function to decrease life when player misses items (when item is outside the area)
- add life text display
- play sound when missing an item
----------------------------
- create end of game function (variable for ready/running/ended) that checks there is no more life
- only generates items if game is running and not ended
- allow START button or SPACE bar key to start a game (generation starts)
- display start message and END message
- play sound when game is finished (may be two sounds, 1 when finished, 1 when high socre is exceeded)
----------------------------
- read a file to get high score : + display high scores at game start/end
- update (save) high scores in file when a game is ended


============================================
FEATURES
============================================
time freeze (time-limited) + display icon/gauge/cooldown
----------------------------
higher speed (time-limited) + display icon/gauge/cooldown


============================================
DISPLAY
============================================
add life bar sprites + function to display bar according to life variable
----------------------------
Add particle emitter or sprite animation when catching an item
----------------------------
Add particle emitter or sprite animation when missing an item
----------------------------
Add parallax system (compute offset for each layer + display in order + exclude some displays depending on game play)

